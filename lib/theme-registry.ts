// Theme Registry - This file is deprecated and will be removed
// The theme system has been replaced with a block-based template system

// Legacy theme registry kept temporarily for backward compatibility
export const themeRegistry = {};

// Helper function to get theme component - returns null as themes are deprecated
export function getThemeComponent(theme: string, type: 'sections' | 'blocks', componentName: string) {
  return null;
}

// Available themes list - empty as themes are deprecated
export const availableThemes = [];

export default themeRegistry;